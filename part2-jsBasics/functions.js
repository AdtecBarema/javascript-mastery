//function

function addNumbers(number1, number2){
return number1+number2;
}

console.log(addNumbers(20.0, 10))

function addNumbers(number1, number2){
    return number1+number2;
    }

const addNumbers2 = (a, b)=>{
   return a+b;
    }

    const multiplication = (a, b)=>{
        return a*b;

         }

    console.log( "result from second function: "+addNumbers2(20.0, 10))

    console.log( "result from multiplication function: "+multiplication(20.0, 10))

    var person= {
        name: "Addisu"

    }

    //Builtin functions 
    console.log(Object.values(person))

    console.log ("Addisu".toUpperCase())

    console.log ("Addisu".toLowerCase())

    console.log ("Addisu".indexOf('i'))


